## Debug log

1. About rdf, check the histogram constructing part.

2. Memory error in NF is because the set pbc part did not set correct bins as boundary bin, so the probing bin is out of the box.

## Parallel note

