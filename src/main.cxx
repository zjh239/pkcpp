// polikit.cpp
// Before using c++20 module features, standard library need to be compiled into module:
// g++ -std=c++20 -fmodules-ts -xc++-system-header iostream

#include <iostream>

import init;
import para;
import nf;

int main(int argc,char** argv){

    initialize(argc, argv);
    // test();

    return 0;
};
